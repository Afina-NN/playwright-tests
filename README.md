<!-- 1. UI-автотесты - обернуты в step (для более понятных отчетов при прогоне через терминал) и describe (для группировки тестов и ускорения прогона)

    1.1 hse.spec.ts - автотесты
        hse.ts - фикстура

    1.2 mgu.spec.ts - автотест
        mgu.ts - фикстура

2. API-автотесты

    2.1 training.api.spec.ts - учебный проект

3. Создан контейнер (docker) для локального запуска

4. К проекту прикручен отчет Allure

5. Файлы allure-results и allure-report лежат в .gitignore -->

